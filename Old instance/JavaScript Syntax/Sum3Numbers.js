function sum(firstNum, secNum, thirdNum) {
    let firstNumber = +firstNum;
    let secondNumber = +secNum;
    let thirdNumber = +thirdNum;
    let result = firstNumber + secondNumber + thirdNumber;
    console.log(result);
}